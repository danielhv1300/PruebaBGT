version: '3.8'
services:
  relational-db:
    image: postgres:latest
    environment:
      - POSTGRES_DB=BTG
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
    ports:
      - "5433:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql